#include <iostream>
#include <cstdlib>
#include <cstring>
#include <vector>


bool isValid(const char* str);
bool gameOver(int arr[3][3], int* winner);
bool putBoardPosition(int number, int arr[3][3], int player);

